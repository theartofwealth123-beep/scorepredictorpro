# netlify.toml ← CLEAN 2025 SYNTAX, NO REDIRECTS NEEDED
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["jstat", "twitter-api-v2"]

# Scheduled scraper every 12 hours
[functions.schedules]
  scheduler = "0 */12 * * *"