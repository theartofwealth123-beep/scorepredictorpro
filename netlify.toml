# netlify.toml — MINIMAL 2025, SCHEDULE MOVED TO FUNCTION CODE
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["jstat", "twitter-api-v2", "stripe", "jsonwebtoken"]